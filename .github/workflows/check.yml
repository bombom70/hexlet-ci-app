name: check
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repo
        uses: actions/checkout@v7
      - uses: actions/setup-node@v7
      - run: make setup
      - run: make lint
      - run: make test
